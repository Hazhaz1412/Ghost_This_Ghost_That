public class CardCollection
{
    public static readonly TCardData AM_BINH = new(
        TCardTypeEnum.GHOST,
        nameof(AM_BINH),
        "ÂM BINH",
        "Binh hồn xuất khiếu",
        3,
        2,
        2
    );

    public static readonly TCardData MA_CANG_SUNG = new(
        TCardTypeEnum.GHOST,
        nameof(MA_CANG_SUNG),
        "MA CÀNG SÙNG",
        "Gào xé âm ti",
        5,
        2,
        5
    );

    public static readonly TCardData MA_CO = new(
        TCardTypeEnum.GHOST,
        nameof(MA_CO),
        "MA CƠ",
        "Rủ rỉ gọi mời",
        3,
        2,
        3
    );

    public static readonly TCardData MA_DA = new(
        TCardTypeEnum.GHOST,
        nameof(MA_DA),
        "MA DA",
        "Kéo chân dưới nước",
        2,
        3,
        1
    );

    public static readonly TCardData MA_DOI = new(
        TCardTypeEnum.GHOST,
        nameof(MA_DOI),
        "MA ĐÓI",
        "Há miệng đòi ăn",
        0,
        3,
        2
    );

    public static readonly TCardData MA_VU_DAI = new(
        TCardTypeEnum.GHOST,
        nameof(MA_VU_DAI),
        "MA VÚ DÀI",
        "Vỗ về từ âm giới",
        2,
        2,
        3
    );

    public static readonly TCardData MA_GA = new(
        TCardTypeEnum.GHOST,
        nameof(MA_GA),
        "MA GÀ",
        "Gáy gọi tai ương",
        1,
        2,
        1
    );

    public static readonly TCardData MA_LAI = new(
        TCardTypeEnum.GHOST,
        nameof(MA_LAI),
        "MA LAI",
        "Huyết mạch ly thân",
        3,
        5,
        5
    );

    public static readonly TCardData MA_LON = new(
        TCardTypeEnum.GHOST,
        nameof(MA_LON),
        "MA LON",
        "Rút ngãi đổi mạng",
        1,
        1,
        1
    );

    public static readonly TCardData MA_MAT_MAM = new(
        TCardTypeEnum.GHOST,
        nameof(MA_MAT_MAM),
        "MA MẶT MÂ ",
        "Vỡ mâm đoạt mạng",
        2,
        3,
        1
    );
}
